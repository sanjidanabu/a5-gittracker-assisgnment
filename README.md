1️⃣ What is the difference between var, let, and const?
ans:var- var is the old javascript way of declaring variable.
1.can be redeclare
2.can be re-assign
3.can be hosting
let-let is modern javascript
1.can't be redeclare
2.can't be re-assign
const: a const variable is declared with const.
1.can't be redeclare
2.can't be re-assign
2️⃣ What is the spread operator (...)?
Ans: spread operator is used to copy or expand the data inside an array or object.
3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: foreach()-foreach simply loops over each element of the array.it does not return a new array.
filter()-filter selects some elements according to the condition and returnss a new array.
map()-map operates on each element of an array and returns a new array.
4️⃣ What is an arrow function?
Ans: Arrow function is a shorthand syntax of Es6,which allows you to write functions in javascript a function in javascript in a short and simple way.
5️⃣ What are template literals?
Ans: Template Literals are a modern way of writing strings in JavaScript, using backtick ( ).It allows you to easily use variables, expressions, and multi-line strings.